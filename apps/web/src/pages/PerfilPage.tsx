import { PlaceholderPage } from "../components/layout/PlaceholderPage";

export function PerfilPage() {
  return (
    <PlaceholderPage
      titulo="Perfil operacional"
      descricao="Produtor, equipe, infraestrutura e capital."
      fase="Cadastro de perfil_produtor/equipe chega na Fase 5 — schema já migrado (docs/specs/01, camada D)."
    />
  );
}
