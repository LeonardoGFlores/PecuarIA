import { PlaceholderPage } from "../components/layout/PlaceholderPage";

export function OfertaPage() {
  return (
    <PlaceholderPage
      titulo="Oferta regional"
      descricao="Animais, insumos, serviços e condições logísticas."
      fase="CRUD completo chega na Fase 5 — schema já migrado (docs/specs/01, camada E); catálogo básico de fornecedores já disponível via API."
    />
  );
}
